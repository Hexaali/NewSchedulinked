/** @type {import('next').NextConfig} */
const nextConfig = {

    // basePath: '/7oaks',
    // assetPrefix: '/7oaks/',
    images: { unoptimized: true },
    output: 'export',

};

export default nextConfig;
